/** @type {import('tailwindcss').Config} */
export default {
  // En Tailwind CSS v4, 'content' ya no es necesario aquí, se detecta automáticamente desde el CSS.
  // Las animaciones personalizadas ahora se definen en el archivo CSS usando @theme.
  plugins: [],
};